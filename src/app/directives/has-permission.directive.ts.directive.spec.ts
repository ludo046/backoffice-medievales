import { HasPermissionDirectiveTsDirective } from './has-permission.directive.ts.directive';

describe('HasPermissionDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPermissionDirectiveTsDirective();
    expect(directive).toBeTruthy();
  });
});
